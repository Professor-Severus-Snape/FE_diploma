import NextPassenger from '../NextPassenger/NextPassenger';
import './documentsNotChecked.css';

const DocumentsNotChecked = () => {
  return (
    <div className="documents-not-checked">
      <NextPassenger />
    </div>
  );
};

export default DocumentsNotChecked;
